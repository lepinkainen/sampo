server:
  port: 8080

cache:
  dir: ".cache"

roots:
  - name: "Sample"
    path: "./testdata"

# Person detection (opt-in, requires ONNX Runtime installed)
detection:
  enabled: false
  model_path: "models/yolov8n.onnx"
  model_version: "v8n-1.0"
  threshold: 0.5
  workers: 2
